name: Run Tests

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Build compiler
        run: |
          cmake -B build
          cmake --build build

      - name: Run Tests from Book
        run: |
          chmod +x ./test_hook
          ./test_hook

      - name: Run Custom Tests
        run: |
            chmod +x ./custom_test_hook
            ./custom_test_hook
